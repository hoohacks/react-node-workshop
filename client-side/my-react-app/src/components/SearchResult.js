import React from 'react';

function SearchResult({ title }) {
    return <div> <h1>{title}</h1></div> // title is a property of the search result and we are displaying it as a header on the webpage
}

export default SearchResult;